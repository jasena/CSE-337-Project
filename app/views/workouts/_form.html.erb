<%= form_for(@workout) do |f| %>
  <% if @workout.errors.any? %>
    <div id="error_explanation">
      <h2><%= pluralize(@workout.errors.count, "error") %> prohibited this workout from being saved:</h2>

      <ul>
      <% @workout.errors.full_messages.each do |msg| %>
        <li><%= msg %></li>
      <% end %>
      </ul>
    </div>
  <% end %>

  <!--<% kinds_choices = ["Cardio", "Weight lifting"] %>
  <div class="field">
    <%= f.label :name %><br />
    <%= f.text_field :name %>
  </div>
  <div class="field">
    <%= f.label :kind %><br />
    <%= f.select(:kind, kinds_choices) %>
  </div>
  <div class="field">
    <%= f.label :workout_date %><br />
    <%= f.date_select :workout_date %>
  </div>
  <div class="field">
    <%= f.label :duration %><br />
    <%= f.text_field :duration %>
  </div>
  <div class="actions">
    <%= f.submit %>
  </div>-->
<% end %>

<hr>
<br>

<div class="span10" style="">
	<% kinds_choices = ["Cardio", "Weight lifting"] %>
  <div class="span9">
    <%= form_for(@workout, :html => { :class => "form-horizontal" } ) do |f| %>
        <%= f.hidden_field :user_id %>
    <div class="control-group">
      <%= f.label :name, class: "control-label" %>
      <div class="controls">
        <%= f.text_field :name, :class => 'input-large' %>
      </div>
    </div>
    <div class="control-group">
      <%= f.label :kind, class: "control-label" %>
      <div class="controls">
        <%= f.select(:kind, kinds_choices) %>
      </div>
    </div>
    <div class="control-group">
      <%= f.label :workout_date, class: "control-label" %>
      <div class="controls">
        <%= f.date_select :workout_date, :class => 'input-large' %>
      </div>
    </div>
    <div class="control-group">
      <%= f.label :duration, class: "control-label" %>
      <div class="controls">
        <%= f.text_field :duration, :class => 'input-large' %>
      </div>
    </div>
    <div class="control-group">
      <div class="controls">
        <%= f.button :submit %>
      </div>
    </div>
    <% end %>
  </div>
</div>


